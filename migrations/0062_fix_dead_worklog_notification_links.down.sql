-- Deliberately not reversible: putting a dead link back into people's inboxes
-- is not a state worth restoring, and the rows it would target can no longer be
-- told apart from notifications that always pointed at /ta.
SELECT 1;
